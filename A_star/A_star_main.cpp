#include "A_star.h"

int main(){
    
    auto board = ReadBoard("grid.txt"); // Path to the grid file
    // PrintGrid(board);
    // array<array<int,2>,2> StartGoalPoints=StartGoal(board);
    // bool cell=CheckValidCell(0,0,board);
    // cout<<cell<<"\n";
    // vector<vector<int>> openlist{{1, 2, 3, 4}};
    // PrintGridd(openlist);
    // AddToOpen(5, 3, 7, 8, openlist, board);
    // PrintGrid(board);
    // int g = 0;
    // ExpandNeighborhood(StartGoalPoints, board, g, openlist);
    // // PrintGrid(board);
    // PrintGridd(openlist);

}
